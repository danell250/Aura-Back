# MongoDB Configuration
# For production, use MongoDB Atlas connection string:
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/aura?retryWrites=true&w=majority

# For local development, use local MongoDB:
MONGO_URI=mongodb://localhost:27017/aura

# Alternative: Use MongoDB Atlas free tier (recommended for production)
# 1. Create account at https://cloud.mongodb.com
# 2. Create a free cluster
# 3. Get connection string and replace above
# Example: MONGO_URI=mongodb+srv://user:pass@cluster0.xxxxx.mongodb.net/aura?retryWrites=true&w=majority

# Server Configuration
PORT=5002
NODE_ENV=development

# CORS Configuration
# Add VITE_FRONTEND_URL for consistency with frontend config
VITE_FRONTEND_URL=http://localhost:5173
VITE_BACKEND_URL=http://localhost:5002

ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5000,https://auraradiance.vercel.app,https://auraraidiate.vercel.app
